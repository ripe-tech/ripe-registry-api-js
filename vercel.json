{
    "version": 2,
    "builds": [
        {
            "src": "package.json",
            "use": "@now/static",
            "config": {
                "distDir": "dist"
            }
        }
    ],
    "routes": [
        { "src": "/", "dest": "/docs/index.html" }
    ]
}
